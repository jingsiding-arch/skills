# 标题

## 一、需求背景
<callout background-color="light-blue">
现有 xx 只支持 xx，无法满足 xx 的诉求；
xx 流程只支持 xx，无法覆盖 xx 场景；
xx 节点发现 xx 时，缺少 xx 闭环。
</callout>

## 二、需求方案描述
### 2.1 页面名称 / 页面路径

<!-- 仅复杂页面使用下方结构；轻量字段/规则优化优先直接写 1-3 条短 bullet -->
| 改动点 | 用户要做什么 | 系统要保证什么 |
| --- | --- | --- |
| 改动点一 |  |  |

#### 2.1.1 改动点一

**页面表现**
- 入口 / 展示：
- 操作反馈：

**规则口径**
- 生效条件：
- 核心规则：
- 异常处理：

#### 2.1.2 操作流程
- 仅当存在跨页面、跨角色、跨状态、审批链路、批量处理、异常分支或回退路径时保留；否则删除本小节。
- 流程说明：优先使用飞书流程图组件展示；无法落图时保留 Mermaid 草稿或 `流程图待落白板` 标记。

#### 2.1.3 权限控制
- 仅当新增入口/操作能力，或改变可见范围、可操作范围、角色边界时保留；否则删除本小节。
- 权限说明：
